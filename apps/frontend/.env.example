# URL do backend usada pelo navegador (imagens, login, cadastro)
NEXT_PUBLIC_API_URL=http://localhost:4001
# Endereço público do site (vai no link da peça na mensagem do WhatsApp)
NEXT_PUBLIC_SITE_URL=http://localhost:4003
# URL do backend usada pelo servidor do Next (opcional; padrão: NEXT_PUBLIC_API_URL)
# API_URL=http://localhost:4001
